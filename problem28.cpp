#include<iostream>
#include<vector>
#include<set>
#include<string>
#include<bits/stdc++.h>
#include<map>

#include "timer.h"

using namespace std;

int main()
{
	ExecutionTimer tmr;
	vector < vector < int > > spiral;
	for(int i = 0; i < 1001; i++){
		vector < int > temp(1001);
		spiral.push_back(temp);
	}
	
	
	return 0;
}
